# CLAUDE.md — 다한증 사용자를 위한 금융 앱 프로토타입

> 이 문서는 Claude Code 핸드오프용 구현 스펙이다.
> 배경 리서치는 `01_페르소나_리서치_정리본.md` 참고. 이 문서만으로 구현 가능하도록 작성됨.

## 0. 프로젝트 개요

- **과제**: 대학 수업 "1인을 위한 앱 리디자인" — 극심한 손 다한증을 가진 24세 사용자 '김진우' 단 한 명을 위한 카카오뱅크 리디자인
- **핵심 컨셉**: 빠른 송금이 아니라 **"제대로 보냈다는 확신"** 을 설계한다
- **산출물**: 모바일 화면 비율의 고퀄리티 인터랙티브 웹 프로토타입 (발표 시연용)
- **품질 기준**: 실제 금융 앱처럼 보일 것. 더미 데이터지만 인터랙션·애니메이션·햅틱 피드백은 진짜처럼 동작할 것

## 1. 기술 스택

- **Next.js 15 (App Router) + React 19 + TypeScript + Tailwind CSS** (팀 표준 스택)
- 상태: zustand 또는 React Context (잔액·거래내역·모드 토글 공유)
- 애니메이션: framer-motion (게이지, 슬라이드, 완료 모션에 적극 사용)
- 햅틱: `navigator.vibrate()` + 미지원 환경(데스크톱) 폴백으로 화면 펄스/링 애니메이션 동시 제공
- 배포 전제 없음. `npm run dev` 로 로컬 시연

## 2. 레이아웃 원칙 (전 화면 공통, 페르소나 직결)

이 규칙들이 이 프로젝트의 정체성이다. 모든 화면에서 일관되게 지켜라.

1. **모바일 프레임**: 데스크톱에서 열어도 중앙에 390×844 비율의 폰 프레임으로 렌더. 프레임 밖은 어두운 배경 + 과제 타이틀
2. **오른쪽 가장자리 우선 배치**: 주요 액션 버튼(다음, 확인, 송금)은 오른손 엄지가 닿는 **우측 하단~우측 가장자리**에 배치. 좌측 하단에는 액션 버튼 금지
3. **뒤로가기 오터치 방지**: 뒤로가기는 **상단 좌측에만** 두고, 하단에는 절대 배치하지 않는다. 누르면 즉시 이동하지 않고 0.3초 확인 하이라이트 후 동작
4. **버튼 최소 크기 56px, 버튼 간 간격 최소 16px**. 숫자 키패드는 키 높이 64px 이상, 키 간 간격 12px 이상
5. **터치 피드백은 3중**: 시각(눌림 + 링 확산) + 햅틱(vibrate 10ms) + 필요 시 사운드 없는 마이크로 모션. 모든 탭 가능 요소에 적용
6. **롱프레스 전면 금지**. 길게 누르기가 필요한 자리는 전부 슬라이드 또는 단일 탭 토글로 대체

## 3. 디자인 방향

- 톤: 신뢰감 있는 미니멀 + 따뜻한 안심. 금융 앱의 차가움 대신 "괜찮아, 잘 보냈어"라는 인상
- 컬러: 카카오뱅크 노란색을 그대로 쓰지 말 것. 독자적인 팔레트 — 딥 네이비/차콜 베이스에 확신·완료 순간에만 터지는 밝은 액센트(예: 민트 or 앰버) 권장. CSS 변수로 토큰화
- 타이포: Pretendard (한글). 금액 표시는 크고 굵게 — 입력 중인 금액이 화면에서 가장 큰 요소여야 함
- generic한 보라 그라데이션, 기본 시스템 폰트 느낌 금지. "한 명을 위해 디자인된 앱"이라는 인상이 들도록 디테일에 집중

## 4. 화면 명세

### 4.1 홈 위젯 (시연 진입점) — Solution 4
폰 잠금화면/홈화면을 모사한 화면 위에 위젯 카드가 떠 있는 형태.

- 위젯 내용: 잔액, 최근 거래 상대, 거래 시간
- **기본 상태: 마스킹** (`●●●,●●●원`, 거래 상대는 `김●진`)
- **탭 한 번 → 마스킹 해제, 5초 후 자동 재마스킹** (남은 시간을 얇은 프로그레스 링으로 표시)
- 마스킹 해제 상태에서 `계좌 복사` 버튼 노출 → 탭 시 클립보드 복사 + "복사됨" 토스트 + 햅틱
- 입금이 발생하면(시연용 버튼으로 트리거) 위젯에 🎁 선물상자 + 흔들림 애니메이션 → 탭하면 입금 상세
- 위젯 탭 → 앱 인증 화면으로 진입

### 4.2 인증 화면 (실패 시나리오 없음)
- 첫 실행 시 인증 수단 선택: **지문 / Face ID / 패턴** 카드 3개 중 선택, 이후 기본값으로 저장
- 시연에서는 어떤 수단이든 탭 → 1초 성공 애니메이션 → 홈. **실패 플로우는 만들지 않는다** (교수님 피드백: 실패 과정 제거)
- 성공 시 햅틱 + 체크 모션으로 "성공 경험"을 또렷하게

### 4.3 홈 화면
- 상단: 잔액 카드 (마스킹 토글 동일 적용)
- 우측 하단: 큼직한 `보내기` FAB (엄지 영역)
- 최근 거래 3건 리스트 (상대·금액·시간·상태 체크 아이콘)
- 우측 상단: `간단 모드` 토글 스위치 (4.6 참고)

### 4.4 송금 플로우 — Solution 1 + 2 (이 프로토타입의 하이라이트)
**Step 1. 받는 사람**: 최근 송금 상대를 큰 카드 그리드로. 검색은 보조
**Step 2. 금액 입력**:
- 커스텀 숫자 키패드 (키 높이 64px+, 간격 12px+, 우측 정렬 레이아웃)
- **중복 입력 방지**: 동일 키가 250ms 이내 재입력되면 무시하고, 키가 살짝 흔들리는 모션 + "한 번만 입력됐어요" 마이크로 라벨로 알려줌 (사용자가 시스템을 신뢰하도록 무시 사실을 숨기지 않는다)
- 백스페이스에도 동일 디바운스 적용
- 입력 중 금액을 화면 최상단에 초대형으로 + **한글 보조 표기** ("오십만 원") — 50,000 vs 500,000 오인 방지
- 키 입력마다 햅틱 10ms + 키 펄스
**Step 3. 확인 + 슬라이드 투 센드**:
- 받는 사람·은행·금액(숫자+한글)을 카드로 크게 재표시
- 하단에 **슬라이드 바**: 우측으로 끝까지 밀면 송금. 진행에 따라 게이지가 차오르고 색이 액센트로 변하는 애니메이션. 중간에 놓으면 부드럽게 복귀
- 슬라이드 완료 순간 강한 햅틱(50ms) + 화면 전체 확산 모션
**Step 4. 완료 화면 (확신의 절정)**:
- 큰 체크 애니메이션 + 진동
- 거래 정보 강조 카드: 받는 사람 / 금액 / **보낸 시각(초 단위)** / 거래 상태 "완료"
- 문구 톤: "김진우님, 박민서님께 50,000원을 정확히 보냈어요"
- 우측 하단 `홈으로`, 그 위에 `영수증 보기`

### 4.5 거래내역 + 물리 버튼 시뮬레이션 — Solution 3 일부
- 거래 카드 1건씩 크게 보여주는 카드 스택 뷰 (확신용 재확인에 최적화)
- **볼륨 버튼 시뮬레이션**: 폰 프레임 좌측에 실제 볼륨키 모양 UI를 렌더하고, 클릭(또는 키보드 ↑↓)으로 거래 카드를 넘김 → "터치 없이 재확인" 컨셉 시연
- 각 거래에 상태 체크 + 시각 표시

### 4.6 간단 모드 — Solution 3
- 홈 우측 상단 토글로 **사용자가 직접** 켜고 끔. 자동 진입 절대 없음
- 켜면: 화면에 `보내기 / 잔액 / 거래내역` 3개의 초대형 버튼만 남는 레이아웃으로 전환 (전환 모션 포함)
- 송금 플로우도 간단 모드에서는 최근 상대 4명 + 키패드 + 슬라이드만으로 압축
- 명칭은 반드시 "간단 모드" ('보호 모드' 금지 — 낙인 우려로 팀에서 변경 확정)

### 4.7 시연용 데모 패널 (선택이지만 권장)
폰 프레임 바깥(데스크톱 영역)에 발표용 컨트롤:
- `입금 발생시키기` (위젯 선물상자 트리거)
- `중복 입력 시뮬레이션` (키패드에서 같은 키 2연타를 자동 재생해 방지 동작을 보여줌)
- 햅틱 발생 시 데스크톱에서도 보이도록 프레임 테두리 펄스

## 5. 더미 데이터

```ts
const user = { name: "김진우", account: "카카오뱅크 3333-12-3456789", balance: 1_284_500 };
const recentContacts = ["박민서", "이서연", "최준호", "정다은"]; // 은행/계좌 임의
const transactions = [
  { type: "송금", to: "박민서", amount: -50_000, time: "오늘 12:34:08", status: "완료" },
  { type: "입금", from: "이서연", amount: +210_000, time: "오늘 09:12:41", status: "완료" },
  { type: "송금", to: "최준호", amount: -15_000, time: "어제 19:05:22", status: "완료" },
];
```

## 6. 하지 말 것 (페르소나·피드백 기반 금지 목록)

- ❌ 롱프레스 인터랙션 일체
- ❌ 지문 실패 → 다른 인증 전환 같은 실패 시나리오
- ❌ 비정상 터치 자동 감지 → 모드 자동 전환 (불안형 사용자에게 역효과)
- ❌ "보호 모드"라는 명칭
- ❌ 하단 좌측 액션 버튼, 하단 뒤로가기
- ❌ 작은 버튼·촘촘한 리스트 등 일반 금융 앱 밀도 그대로 복제

## 7. 완료 기준 (Definition of Done)

1. 홈 위젯 → 인증 → 홈 → 송금 4단계 → 완료 → 거래내역 재확인까지 끊김 없이 시연 가능
2. 중복 입력 방지가 실제로 동작하고 사용자에게 시각적으로 안내됨
3. 슬라이드 투 센드 게이지·완료 햅틱·체크 애니메이션이 부드럽게 동작
4. 간단 모드 토글 전환이 모션과 함께 동작
5. 위젯 마스킹 탭 토글 + 5초 자동 재마스킹 + 계좌 복사 동작
6. 모바일 뷰포트(390px)와 데스크톱(폰 프레임) 모두에서 깨지지 않음
